import { GlobalStyles } from '@mui/material'

const globalStyles = (
    <GlobalStyles
         
        styles={() => ({
            body: {
                overflowY: 'scroll',
            },
        })}
    />
)

export { globalStyles }
